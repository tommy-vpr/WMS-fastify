import { Warehouse } from "lucide-react";
import { PlaceholderPage } from "../../components/PlaceholderPage";

// pages/inventory/index.tsx
export function InventoryPage() {
  return <PlaceholderPage title="Inventory" icon={Warehouse} />;
}
