import { Package } from "lucide-react";
import { PlaceholderPage } from "../../components/PlaceholderPage";

// pages/pack/index.tsx
export function PackPage() {
  return <PlaceholderPage title="Pack Station" icon={Package} />;
}
